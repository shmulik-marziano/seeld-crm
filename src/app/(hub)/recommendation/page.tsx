import { RecommendationReport } from '@/components/hub/recommendation/RecommendationReport';

export const metadata = {
  title: 'סיכום המלצה — SEELD',
};

export default function RecommendationPage() {
  return <RecommendationReport />;
}
