import { RecommendationCreator } from '@/components/hub/recommendation/RecommendationCreator';

export const metadata = { title: 'יצירת המלצה — SEELD' };

export default function CreateRecommendationPage() {
  return <RecommendationCreator />;
}
